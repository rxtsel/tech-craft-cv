export const MAIN_ROUTES = {
  home: "/",
  notFound: "*"
};

export const AUTH_ROUTES = {
  auth: "/auth",
  signIn: "/auth/signin",
  signUp: "/auth/signup"
};

export const DASHBOARD_ROUTES = {
  dashboard: "/dashboard"
};
